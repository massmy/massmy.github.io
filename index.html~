---
layout: default
---
{% for post in site.posts %}
{% if post.category != 'Project'%}
    <div class="well post">
        <a href="{{post.url}}"><h3>{{post.title}}</h3></a>
        {{post.content}}
    </div>
{% endfor %}
