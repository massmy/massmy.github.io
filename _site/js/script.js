$(function() {
    $('pre').addClass("prettyprint linenums");
    prettyPrint();

    //This is going to be some futuristic stuff based on alien technology. Still under construction.
    /*var contentDiv = $('#content');
    $('.nav a').click(function(e) {
        contentDiv.load($(this).attr("href") + " #content");
        e.preventDefault ? e.preventDefault() : event.returnValue = false;
    });*/
});